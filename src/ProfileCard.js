function ProfileCard(props){
    return <div>
        <h2>Profile Card</h2>
        <h5>Title is {props.title}</h5>
        <h5>Social handle is {props.handle}</h5>
        </div>
}

export default ProfileCard;